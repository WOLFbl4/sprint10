- name: Configure all servers (base setup)
  hosts: all
  become: yes
  roles:
    - common

- name: Configure backend servers
  hosts: backend
  become: yes
  roles:
    - nginx_backend

- name: Configure proxy server
  hosts: proxy
  become: yes
  roles:
    - nginx_proxy

