all:
  children:
    managed_nodes:
      hosts:
        srv-01:
          ansible_host: 
          # Мы берем пользователя для облачных ВМ 
          ansible_user: ubuntu
        srv-02:
          ansible_host: 
          # Мы берем пользователя для облачных ВМ 
          ansible_user: ubuntu
        srv-03:
          ansible_host: 
          # Мы берем пользователя для облачных ВМ 
          ansible_user: ubuntu
  vars:
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519
    # Отключаем проверку ключей, так как IP у облачных ВМ часто меняются
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
