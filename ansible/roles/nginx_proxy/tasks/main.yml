- name: Install nginx
  apt:
    name: nginx
    state: present
    update_cache: yes

- name: Configure nginx proxy
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/sites-available/default

- name: Ensure nginx is running
  service:
    name: nginx
    state: restarted
    enabled: yes
